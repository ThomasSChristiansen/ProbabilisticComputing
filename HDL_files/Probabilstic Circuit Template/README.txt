Copy paste these sources into vivado and set 'probabilistic_top_level_template.sv' as the top level source.

Generate mem files for your desired network and add these as well.

Edit the parameters as explained at the top of 'probabilistic_top_level_template.sv' to fit the network.

Edit parameters in 'clamper.sv' to fit the P-bits you wish to clamp

Edit 'grouped_update_order_LUT.sv' to fit the circuit
